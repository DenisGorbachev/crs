## Needs

- A set of programs to display the next task:
  - Must contain a program to display the most narrowly focused task
    - Examples
      - Agent final message
      - Single path diff between the commit A and commit B where:
        - Commit A is the tip of the branch
        - Commit B contains the latest approved version of this path
  - Must contain a program to display the complete [call chain](#call-chain)
    - Rationale: the user wants to know the reason for the current task
- A set of programs to help with completing the tasks
  - #examples
    - Push a message part
    - Show a message

## Definitions

### Call chain
